# Configuración mínima para backend productivo fuera de Docker.
# Si usas Docker Compose, define estas variables en la raíz del proyecto (.env).

DB_HOST=mysql
DB_PORT=3306
DB_USER=gymtec_user
DB_PASSWORD=change-this-app-password
DB_NAME=gymtec_erp

PORT=3000
NODE_ENV=production
JWT_SECRET=change-this-jwt-secret
CORS_ORIGIN=https://tu-dominio.com
